{% extends 'base.html.twig' %}

{% block title %}New Home{% endblock %}

{% block body %}
    <h1>Create new Home</h1>
    <a href="{{ path('home_index') }}" class="btn btn-dark">Retour à la liste</a>

    {{ include('home/_form.html.twig') }}

{% endblock %}
